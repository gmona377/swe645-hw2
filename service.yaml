apiVersion: v1
kind: Service
metadata:
  name: swe645-hw2-deployment-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    workload.user.cattle.io/workloadselector: apps.deployment-default-swe645-hw2-deployment
  ports:
    - name: hw2-service
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31485
